spring:
  application:
    name: slackbot
    
server:
  port: 9119

slack:
  bot:
    key: yourkey
    channel: paulsnotes
    name: mycroft
    
mycroft:
  uri: ws://192.168.0.177:8000/events/ws
  
logging:
  level:
    slacker: DEBUG
    com.ullink.slack: INFO
    
endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: true
