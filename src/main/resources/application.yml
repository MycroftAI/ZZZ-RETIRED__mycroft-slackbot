spring:
  application:
    name: slackbot
    
server:
  port: 9119

slack:
  bot:
    key: yourkey
    channel: random
    name: mycroft
logging:
  level:
    slacker: DEBUG
    com.ullink.slack: INFO
    
endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: true
